```javascript
/** 阻止默认事件(按空格键网页滚动) */
      preventScroll(){
        document.body.onkeydown = function(event) {
          var e = window.event || event;
          if (e.preventDefault) {
              e.preventDefault();
          } else {
              window.event.returnValue = false;
          }
        }
      },
```

